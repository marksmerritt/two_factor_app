<nav class="nav">
  <%= link_to "Home", root_path, class: "nav-link" %>
  <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link btn-danger" %>
</nav>

<h1>Welcome, <%= current_user.email %></h1>

<div class="card">
  <h2>Two-Factor Authentication Status</h2>
  
  <% if current_user.two_factor_auth_required? %>
    <p class="status status-required">🔒 Two-Factor Authentication is <strong>required</strong> for your account</p>
  <% else %>
    <p class="status status-optional">🔓 Two-Factor Authentication is <strong>optional</strong> for your account</p>
  <% end %>
  
  <% if current_user.two_factor_enabled? && current_user.two_factor_verified? %>
    <p class="status status-enabled">✓ Two-Factor Authentication is enabled and verified</p>
  <% elsif current_user.two_factor_enabled? %>
    <p class="status status-warning">⚠ Two-Factor Authentication is enabled but not verified</p>
  <% else %>
    <p class="status status-disabled">✗ Two-Factor Authentication is not enabled</p>
    <p class="mt-1">
      <%= link_to "Set up Two-Factor Authentication", two_factor_setup_path, class: "btn btn-primary" %>
    </p>
  <% end %>
</div>

